# CHANGELOG — issue-mr

- **2.0.4** (2026-06-28) — **Mode ANALYSE : recommandation par question.** Chaque question de l'`AskUserQuestion` porte une réponse recommandée par défaut (pas seulement à valeur égale) ; ne pas trancher sans afficher son parti. Bump patch (une phrase).
- **2.0.3** (2026-06-10) — **Interop pipeline (audit de synergie famille)** : renvoi `feature-loop --issue=N` enrichi (rappel que la branche `N-slug` créée ici est reprise) + garde anti-boucle : pas de re-suggestion quand issue-mr a été invoqué depuis le pont feature-loop. Résorbe le décalage de la ligne `skill_version` (2.0.1 → 2.0.3). Bump patch (précisions de renvoi).
- **2.0.2** (2026-06-10) — Mode ANALYSE : la boussole **ETC** (« Grand Livre » ch. 3) ajoutée comme critère d'arbitrage — à valeur égale, recommander l'option qui laisse le système le plus facile à changer. Une ligne, seul apport pertinent du référentiel pour ce skill (il ne produit pas de code). Bump patch.
- **2.0.1** (2026-06-10) — **Préparation au partage équipe (sync vers les copies projet).** Les références à `feature-loop`/`branch-wrap-up` (skills personnels, absents chez les collègues) deviennent conditionnelles (« si disponible ») — le contenu reste une source unique copiée verbatim dans les repos, pas d'édition team-spéciale qui recréerait du drift. Nouvelle note « Copies projet » dans Intégration écosystème : une copie repo est légitime pour l'équipe ; chez un user ayant aussi la globale, la globale prime (précédence documentée personal > project) ; la globale est la source de vérité, re-sync à chaque bump (`SKILL.md` + `CHANGELOG.md`). Déployé sur plusieurs projets réels. Bump patch (formulation, comportement inchangé).

- **2.0.0** (2026-06-10) — **Promotion en skill global + fusion de 4 copies projet divergentes** (plusieurs projets réels — archivées dans `.archive/ancestor-*.md` ; 4 hashes différents, 2,8 → 5,8 Ko, aucune versionnée : drift copier-coller classique). Base retenue : la copie projet la plus évoluée (modes ISOLER/SCAFFOLD, validation fichiers+commit, description MR structurée, garde branche protégée). Changements de contrat : (1) **Données projet découvertes, plus jamais hardcodées** — labels via `glab/gh label list` (interdit d'inventer un label absent), branche par défaut via CLAUDE.md puis `git remote show origin`, scopes conventionnels via `git log` ; les anciennes listes de labels par projet deviennent inutiles (déclarables dans le CLAUDE.md du repo si on veut les épingler). (2) **Dual-plateforme GitLab (`glab`) / GitHub (`gh`)** auto-détectée du remote ; GitHub : `gh issue develop` pour la branche liée, gotcha PR-sans-commit documenté (PR différée en SCAFFOLD). (3) **Mode ANALYSE ajouté** (`--analyse`, tâche floue, ou pont feature-loop) : exploration du code (déléguée si volumineuse), questions de conception tranchées avec l'user, corps d'issue structuré Constat/Pourquoi/Périmètre/Plan/Risques — le chaînon que le pont feature-loop 8.3.0 décrivait (« mode analyse ») mais qu'AUCUNE des 4 copies n'implémentait. L'issue produite passe d'office le test de clarté de `feature-loop --issue=N`. (4) **`disable-model-invocation` abandonné** (présent sur 2 copies ; il aurait cassé l'invocation par le pont feature-loop) — le garde-fou est ailleurs : RIEN n'est créé avant la validation user de l'Étape 3 (titre, labels, branche cible, et en ISOLER : liste de fichiers + message de commit). (5) `allowed-tools` (présent sur 1 copie) abandonné — trop restrictif pour le mode ANALYSE (Agent), la permission harness suffit. (6) Flag `--issue-only`, langue de l'issue alignée sur celles du projet, suggestion `feature-loop --issue=N` en fin de run. Frontmatter conforme (description < 1024, YAML plié, hint quoté).
- **≤ 1.x** (non versionnées, 2025-2026) — Copies par projet, divergentes : l'une portant `disable-model-invocation`, une autre figée en clone, une autre avec `allowed-tools` et target `develop`, et la plus évoluée (Step 0 ISOLER/SCAFFOLD, validation étendue, description MR structurée — issue de runs feature-loop réels).
