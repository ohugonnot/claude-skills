# Feature-loop — meta-leçons cross-projet

Leçons sur *comment piloter la boucle*, valables sur tous les projets (pas les specs d'un projet — ça va dans `project_feature_loop_insights.md`). Chargé à l'init, complété en fin de run et par `learn`.

Format : `- **<titre>** : <règle actionnable> — *vu sur N runs*`.

> ⚠️ **Ce fichier est un SEED VIDE et doit le rester.** La mémoire réelle vit hors dépôt, dans `~/.claude/skill-memory/feature-loop-lessons.md` — c'est ce chemin que lisent l'Étape 3 et l'Étape 5.7. Le skill n'écrit donc jamais dans ce dépôt public. Si des leçons réapparaissent ici, c'est que le découplage a été cassé : les déplacer vers le chemin hors dépôt plutôt que de les anonymiser une fois de plus.

<!-- (vide — la mémoire se remplit en local, hors de ce dépôt) -->
