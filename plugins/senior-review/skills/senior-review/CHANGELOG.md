# CHANGELOG — senior-review

- **1.11.0** (2026-08-14) — **Journal de runs : la mesure de tendance qui manquait.** Les deux mémoires cross-projet livrées en 1.9.0 et 1.10.0 distillent des *règles* — ce qui a marché, ce qui a raté — et perdent au passage la donnée par run. Elles ne peuvent donc pas répondre à « le skill s'améliore-t-il ? », qui est une question de tendance sur N revues, pas de corpus. Nouveau `~/.claude/skill-memory/senior-review-runs.jsonl`, **append-only, une ligne JSON par revue, écrite systématiquement quel que soit le projet, la branche ou le verdict** : date, projet, branche, tier, tour, findings remontés / confirmés / droppés au gate / réfutés au panel, entrées ajoutées en `tranché` et `hors périmètre`, agents, tokens, verdict — tous des champs déjà connus à l'Étape 7, aucun jugement du modèle. **Clés en ASCII sans accent, décidé par le test** : la première version du schéma les portait en français accentué, et `jq` — l'outil avec lequel l'analyse se fera — refuse `.confirmés` en erreur de syntaxe, imposant `.["confirmés"]` à chaque champ. Une clé JSON est une interface machine, pas de la prose ; quatre des cinq agrégations de test échouaient à la compilation, seule celle qui ne touchait que `tokens` et `agents` passait. **Contrainte structurante : ce fichier n'est JAMAIS chargé à l'Étape 1.8.** Il n'informe pas une revue, il sert à les analyser toutes ensemble plus tard ; l'ouvrir en revue serait reproduire exactement le défaut corrigé en 1.9.0 (un fichier lu intégralement à chaque run et croissant sans borne). C'est aussi ce qui permet de s'affranchir de tout plafond : une ligne pèse ~200 octets et n'entre jamais en contexte, donc mille revues coûtent 200 Ko sur disque et zéro token. Aucune analyse n'est livrée avec — il n'y a pas encore de données ; la construire maintenant serait de la sur-ingénierie « au cas où ». Les revues antérieures à cette version ne sont pas reconstituables : seuls les nombres de tours survivent dans les ledgers, pas les comptes de findings, et amorcer le journal avec des lignes partielles fausserait la tendance qu'il est censé mesurer. Bump minor (mémoire nouvelle, aucune étape existante modifiée dans son comportement).

- **1.10.0** (2026-08-14) — **Misses : la mémoire de ce que la revue rate.** Constat mesuré sur les trois ledgers vivants d'un poste réel : 26 entrées en `tranché` et **25 en `hors périmètre`** (11 et 13 sur deux branches, dont l'une en seulement deux tours). Le principe « Scope = le diff » est donc violé assez souvent pour qu'une catégorie entière du ledger existe uniquement pour le rattraper. Or les trois mémoires du skill enregistrent toutes ce qui a **marché** — `senior_review_learnings.md` des faits de code et des faux positifs réfutés, `lessons.md` de la technique de revue, le ledger des arbitrages — et le seul endroit où le signal d'échec existe **meurt à la fusion** : la doctrine 1.8.0 promeut « les faits durables » vers learnings, c'est-à-dire des faits sur le *repo*, jamais sur le *skill*. Symptôme visible dans ce CHANGELOG lui-même : chaque entrée s'ouvre sur « constat sur un run réel », et le constat vient **toujours du dehors** (1.5.0 : trois bugs count↔display trouvés par une revue externe ; 1.6.0 : dérive de nommage attrapée par une relecture humaine a posteriori). Le skill n'a jamais diagnostiqué une seule de ses propres défaillances. Nouveau `~/.claude/skill-memory/senior-review-misses.md`, pendant symétrique de `lessons.md` — *ce qui a marché* / *ce qui a raté*. **Contrainte de conception dictée par le principe n°5** (« l'auto-correction LLM sans oracle dégrade, Huang ICLR 2024 ») : un journal que le skill remplirait de sa propre appréciation violerait sa doctrine et consignerait les mauvaises choses, puisqu'il ignore par construction ce qu'il a manqué. Trois sources externes exclusives, aucune auto-évaluation : `[bruit]` (une entrée est passée en `tranché`/`hors périmètre`), `[manqué]` (un défaut trouvé APRÈS la revue par l'user, la CI ou la prod), `[coût]` (tour ≥ 3 sur la même branche). Lu à l'Étape 1.8 mais **routé uniquement vers « ce qu'on NE flague PAS »**, jamais dans le brief de recherche d'un reviewer — un miss est un filtre, pas une piste, et l'y injecter reviendrait à ancrer le jugement. Départage explicite avec learnings : la règle tient-elle sur un AUTRE projet ? Deux garde-fous tirés des quatre casses du sous-système mémoire (1.2.0 mémoire morte, 1.5.1 fuite, 1.9.0 lecture sans plafond, 1.9.1 écriture en dépôt public) : **plafond 40 entrées** avec fusion obligatoire au-delà, et **hors dépôt dès l'origine**. Critère de complétude vérifiable à l'Étape 7 : chaque entrée ajoutée au ledger ce tour a produit une ligne dans misses **ou** dans learnings. **Le compteur `vu sur N branches` est né d'un test qui a partiellement échoué** : le corpus d'amorçage a été extrait des trois ledgers réels (39 entrées examinées, 16 cross-projet fusionnées en 13 règles), puis on a vérifié combien de ces règles se reproduisaient sur plus d'une branche — **3 sur 13** seulement, dont l'hygiène de mutation sur les 3/3. Une règle vue une fois n'est pas réfutée pour autant, l'échantillon étant de trois branches dont deux très jeunes ; mais le format initial ne distinguait pas « prouvé » de « non prouvé », et chargeait les deux au même titre. D'où le compteur, calqué sur le `vu sur N runs` des lessons, et l'élagage au plafond qui sacrifie les `N=1` les plus anciennes en premier. Bump minor (mémoire et capacité nouvelles ; pipeline, gates, dimensions et aveuglement inchangés).

- **1.9.1** (2026-08-14) — **Mémoire des leçons découplée du dépôt public.** Le scrub de 1.5.1 avait posé un seed vide en décrétant « la mémoire pleine reste locale, hors repo public » — mais rien ne rendait cette règle tenable : `~/.claude/skills/senior-review` est un lien symbolique vers le dossier du plugin, donc `lessons.md` « local » **était** le fichier versionné. Le skill écrivait dans le dépôt public à chaque Étape 7, et la règle a été re-violée dès le run suivant : 90 leçons publiées, puis 99. Anonymiser à chaque commit traitait le symptôme ; la cause est le chemin. Les Étapes 1.8 et 7 lisent et écrivent désormais `~/.claude/skill-memory/senior-review-lessons.md`, hors de tout dépôt, absent au premier run (continuer sans, créer à la première écriture). Le `lessons.md` du dépôt redevient un seed vide, avec un bandeau qui dit quoi faire si des leçons y réapparaissent — signe que le découplage a été cassé, à déplacer plutôt qu'à ré-anonymiser. Aucune leçon perdue : la mémoire réelle a été déplacée avant le vidage. Bump patch (garde anti-fuite et emplacement d'état ; pipeline, gates, dimensions et aveuglement inchangés — même nature que 1.5.1).

- **1.9.0** (2026-08-14) — **Leçons étiquetées par dimension + croissance bornée.** Constat mesuré, pas supposé : l'Étape 1.8 lisait `lessons.md` **intégralement et sans condition**, soit 48,5 Ko qui s'ajoutaient aux 37,6 Ko du SKILL.md — le coût réel d'une revue était donc 86 Ko, et le fichier étant déclaré « additif uniquement » il n'avait aucun plafond. Chaque leçon porte désormais une étiquette de dimension en tête de ligne (`spec correctness security design tests perf harness`), `harness` couvrant les leçons d'orchestration qui ne relèvent d'aucune dimension de code. **Le routage par étiquette n'a PAS été retenu en tier standard** : mesuré sur le corpus réel, une revue standard chargerait encore 95 % du fichier, parce que les leçons se concentrent dans trois étiquettes toujours routées (`harness` 16,2 Ko, `correctness` 15,6 Ko, `tests` 11,3 Ko sur 48,8 Ko) — 5 % de gain ne justifie pas un mécanisme de plus. Le gating est donc réservé à `--quick`, qui ne charge que `harness` + `correctness` (65 % du volume), cohérent avec un palier dont le but est d'être bon marché et qui ne revoit pas les autres dimensions. Étape 7 : l'étiquette devient obligatoire dans le format, et la consigne passe de « additif » à **« chercher d'abord une leçon existante sur le même mécanisme et l'enrichir (incrémenter *vu sur N runs*) plutôt qu'empiler une variante »** — un fichier rechargé à chaque revue ne peut pas croître sans borne. Bump minor (format de leçon et comportement de chargement modifiés, gates, garanties et aveuglement inchangés).

- **1.8.0** (2026-08-12) — **Ledger d'arbitrages par branche.** Constat sur un run réel : une même MR revue douze fois de suite obligeait l'utilisateur à réécrire à la main, avant chaque tour, deux sections — « ce qui est tranché, ne pas remonter » et « ce qui est déjà prouvé par exécution ». Quatre briefs successifs contenaient le même artefact recopié. Les deux mémoires existantes ne couvraient pas ce besoin : `senior_review_learnings.md` est scopé au **repo** et permanent, `lessons.md` au **skill** et cross-projet ; aucune n'est scopée à une **unité de travail**. La règle de re-revue de 1.7.0 (continuer les reviewers via `SendMessage`) ne survivait pas à un `/clear`, qui est précisément le workflow réel entre deux tours. Nouveau `arbitrages-<branche>.md` (Étape 1.9 en lecture, Étape 7 en écriture) portant **trois catégories seulement** — `tranché`, `prouvé`, `hors périmètre` — et **jamais** les findings, sévérités ou verdicts des tours précédents : les relire ancrerait le jugement et tuerait l'aveuglement, qui est la garantie n°1 du skill. D'où la découpe des lecteurs : l'orchestrateur voit les trois catégories, les reviewers ne reçoivent que « ne flague pas ça » + « ne le refais pas ». Une entrée `tranché` reste **réouvrable au prix d'un receipt exécuté** — asymétrie délibérée, sans elle le ledger fige une erreur au lieu d'éviter une redite (vérifié sur le run d'origine : un arbitrage marqué « ne pas remonter » a été respecté et un bloquant adjacent est quand même sorti). Garde-fou anti-overhead : **rien n'est écrit au tour 1**, le ledger n'apparaît qu'à partir du tour 2. Cycle de vie borné : il **meurt à la fusion**, après promotion des faits durables vers `senior_review_learnings.md`. Bump minor (capacité nouvelle, gates et garanties existantes inchangées).

- **1.7.0** (2026-07-01) — **Discipline tokens/latence** (transposée de feature-loop, qui l'a mais senior-review non). Constat sur audit croisé : feature-loop trace `subagent_tokens_total` par run et applique 6 tactiques de sobriété explicites ; senior-review n'avait ni principe ni mesure de coût malgré des mécanismes structurels qui économisent déjà (context pack réutilisé, panel ciblé, paliers). Nouveau principe **Discipline tokens/latence** : (a) context pack écrit une fois, réutilisé verbatim (cache-friendly) ; (b) receipts de l'Étape 4 vérifiés par l'orchestrateur lui-même, jamais un agent dédié ; (c) refute-panel ciblé seulement (déjà existant, désormais nommé comme tactique économique) ; (d) sorties d'outils résumées avant d'entrer dans le context pack (Étape 2) ; (e) **re-revue immédiate du même diff dans la même session** → continuer les reviewers via `SendMessage` plutôt que 5 agents aveugles frais (pattern evaluator-optimizer, transposé de feature-loop 8.11.0) — l'aveuglement protège le premier jugement, pas la vérification d'un correctif. Ligne `coût: <N agents, ~M tokens>` ajoutée au template de rapport (Étape 6), calculée en sommant les `subagent_tokens` du run — mesure objective avant/après toute future optimisation, même logique que `subagent_tokens_total` de feature-loop. Bump minor (principe + capacité nouveaux, gates existantes inchangées).
- **1.6.0** (2026-07-01) — **Nommage : test des 3 questions + dérive après consolidation.** Issu d'un run réel (un projet réel) : un refacto anti-duplication a fait fusionner deux fonctions en délégant l'une à l'autre — la survivante gardait un nom pensé pour un seul appelant (`xResend`), devenu trompeur une fois aussi appelée par l'envoi initial (pas seulement le renvoi). Ni le passage senior-review qui avait précédé le refacto, ni le refacto lui-même, n'avaient de check pour ce cas — attrapé seulement par une relecture humaine a posteriori. Dimension design/maintainability enrichie de deux ancres : (1) **test des 3 questions** sur le nommage (le nom dit-il pourquoi/quoi/comment ? sinon renommer plutôt que commenter — §3.1 du référentiel « Grand Livre » ch.3 déjà cité en 1.3.0) ; (2) **dérive de nom après consolidation** — quand un diff fait gagner un nouvel appelant à une fonction existante par délégation/fusion, vérifier que son nom tient toujours la promesse pour TOUS ses appelants. Résorbe aussi le décalage de la ligne « Version courante » du footer (restée à 1.5.1 alors que le header affichait 1.5.3). Bump minor (critère nouveau, archi inchangée).
- **1.5.3** (2026-06-28) — **Lentille design : tests opérationnels.** Dimension design enrichie de deux heuristiques actionnables (test de suppression = pass-through inutile ; couture réelle = 2 implémentations, une seule = hypothétique) sous l'étiquette YAGNI. Bump patch (un tell, archi inchangée).
- **1.5.2** (2026-06-28) — **Correction de citations.** Citations imprécises / mal attribuées corrigées (audit fact-check des références). Bump patch (références, aucun changement de comportement).
- **1.5.1** (2026-06-16) — **Scrub fuite d'info + garde anti-récurrence** (passe partagée avec feature-loop 8.14.1). `lessons.md` réinitialisé au seed vide (la mémoire pleine reste locale, hors repo public). Format de leçon : retrait du `(<slugs>)` (vecteur de fuite de noms de projets) ; Étape 7 : **anonymisation obligatoire** des leçons versionnées (jamais de nom client/vendor/branche). Footer de version corrigé (1.2.0 → 1.5.1, décalage préexistant). Bump patch (contenu + garde, archi inchangée).
- **1.5.0** (2026-06-12) — **Lentille « cohérence inter-couches »** (post-mortem d'un run réel : 3 bugs count↔display ratés par la passe standard puis trouvés par une review externe — SQL qui COMPTE un ensemble, mapping applicatif qui en AFFICHE un sous-ensemble). Cause racine triple : (a) aucune dimension ne nommait le pattern (correctness était intra-fonction), (b) la gate receipts droppait les findings dont la preuve exige de fabriquer une donnée multi-entités, (c) une leçon antérieure sur la propagation de flag avait focalisé la review sur la transmission, pas la sémantique aval. Correctifs : (1) dimension correctness — lentille obligatoire count↔display / granularité de jointure (par-ligne ≠ par-entité) / symétrie de scope SQL↔applicatif dès que le diff touche une requête agrégée ou un mapping post-requête ; (2) Étape 4 — exception au drop pour ces findings (remontés `🟡 à vérifier manuellement` avec scénario de données, au lieu d'être perdus faute de receipt rapide) ; (3) rubrique Correctness — ancre data-shape (rejouer sur 0/1/PLUSIEURS entités même clé). Bump minor (lentille nouvelle, archi inchangée).
- **1.4.0** (2026-06-10) — **Interop pipeline (audit de synergie famille)** : §6 suggère `branch-wrap-up --no-review` sur verdict `approve` quand la cible est du travail local non clôturé — ferme la chaîne cadrer→implémenter→relire→clôturer. Résorbe le décalage de la ligne `skill_version` (restée à 1.2.0 alors que les contenus 1.3.0 étaient livrés). Bump minor (nouveau renvoi de sortie).
- **1.3.0** (2026-06-10) — **Alignement « Grand Livre » ch. 3** (même passe que feature-loop 8.7.0, charte du code). (1) Dimension design enrichie : boussole **ETC** (« plus facile ou plus dur à changer ? »), commentaire pénible à écrire = signal d'abstraction ratée, **complexité poussée aux appelants** (check répété chez chaque appelant → le module devrait l'absorber : borner, défaut, null object). (2) Deux items ajoutés à « ce qu'on NE flague PAS », ciblant deux réflexes classiques de reviewer LLM : la **duplication identique par hasard** dont les sites évolueront séparément (DRY = savoir, pas texte — fusionner couplerait à tort), et la **fonction longue mais linéaire et claire** (fonction profonde > fonction courte ; ne suggérer un découpage que s'il simplifie l'appelant). Bump minor (critères nouveaux, archi inchangée).
- **1.2.0** (2026-06-10) — **Fermeture de la boucle mémoire + conformité spec + tiers sémantiques** (audit expert externe, même passe que feature-loop 8.6.0). (1) **Bug réel corrigé — mémoire morte** : `lessons.md` était écrit (Étape 7) mais JAMAIS lu — aucune étape ne le chargeait ; les leçons (ex. routage par clé d'agrégat) n'informaient aucune revue future. Nouvelle Étape 1.8 : lecture + injection dans le context pack, routée par dimension (une leçon "tests" nourrit le reviewer tests). (2) **Description frontmatter 1323 → ~830 caractères** (plafond spec Agent Skills = 1024), unifiée en anglais, dit QUAND déclencher pas COMMENT ça marche ; `argument-hint` complété (`--staged`, `--no-tools`). (3) **Tiers de modèles sémantiques** : orchestrateur = modèle de la session ; Haiku/Sonnet/Opus = mapping courant des tiers rapide/standard/max (robuste à Fable et aux générations suivantes) ; honnêteté mono-famille sur les refuteurs (famille ≠ via MCP si dispo, sinon prompt reformulé + ordre inversé). (4) **Attribution baseline au gate** (Étape 2) : un échec d'outil n'incrimine le diff que s'il n'existe pas déjà sur la base — échec préexistant exclu du verdict (scope = le diff) mais signalé ; rejeu sur worktree jetable pour `--base`/`--pr`. Pendant de la leçon feature-loop 8.3.1 (« la baseline doit lancer la même suite que le gate »). (5) **Garde diff vide** (Étape 0) : cible vide → le dire et stop. (6) **Extraction progressive-disclosure** : CHANGELOG → `CHANGELOG.md`, Sources → `reference/references.md` ; carte des fichiers en tête du SKILL.md. (7) **Option `Workflow`** pour le fan-out des reviewers de dimension (sorties schema-validées sur le format finding) ; l'interactif reste en conversation principale. (8) `lessons.md` : format normalisé (une leçon reformatée en bullet standard). Sections LOCKED intactes au caractère près. Backup pré-restructure : `.archive/`. Bump minor (capacités nouvelles, archi inchangée).
- **1.1.1** (2026-06) — Issu d'un run réel (revue en workflow hybride fix+review sur 3 passes). La revue a fait son taff : un reviewer aveugle a attrapé un vrai trou de fidélité de test (un e2e de routage par entité qui court-circuitait le routage par clé d'agrégat → l'assertion d'effet passait quelle que soit l'entité ; seule une pré-assertion d'event l'attrapait). Leçon "comment reviewer" consignée dans `lessons.md` (tester le routage par clé exige ≥2 entités ; vérifier que l'assertion d'EFFET est elle-même discriminante par mutation). Une seule correction de méthodo ici : **§4 piège du checkout global** — restaurer une mutation par `git checkout` efface aussi les éditions légitimes non commitées du même fichier ; restaurer chirurgicalement. Bump patch (hygiène, archi inchangée).
- **1.1.0** (2026-06) — Issu d'un run réel (revue Shopify + CQRS/ES). Ajout du **mode spécialiste** : l'Étape 1 reconnaît la stack+archi, nomme la combinaison, propose/confirme à l'utilisateur de revoir en **expert senior de la stack**, et chaque reviewer reçoit la persona experte + les invariants/pièges de l'archi (CQRS/ES : idempotence, immutabilité events, cohérence projection↔agrégat ; Shopify : pagination/éviction, E.164, scopes, throttling). Ajout de la **recherche web autorisée en cas de doute** (version/API/framework) — en appui discipliné (source primaire citée + date), jamais en substitut du receipt local ; `WebSearch`/`WebFetch` ajoutés aux tools. Bump mineur (nouvelles capacités, archi inchangée).
- **1.0.1** (2026-06) — Issu d'un run réel (revue d'un module e2e en 2ᵉ passe après feature-loop). La revue a bien trouvé de vrais trous de discriminance (fallback Escape non-discriminant, `waitFor` mort) — design validé. Une seule correction, hygiène : **§4 re-run live propre** — quand vérifier un finding exige de muter temporairement un fichier suivi (config/env pointé vers un serveur de test), restaurer via `git checkout` et ne laisser AUCUN backup parasite (`.bak`/`.orig`) ; vérifier `git status` propre en fin de revue (mes reviewers avaient laissé des `config.js.orig` que l'orchestrateur a dû nettoyer). Bump patch.
- **1.0.0** (2026-06) — Création. Architecture orchestrator + reviewers aveugles décomposés + gate de vérification + synthèse calibrée, fondée sur une revue de littérature (LLM-as-judge, PoLL, Huang ICLR, G-Eval, CriticGPT, Agentless, mutation/TDD-Bench) et l'archi des meilleurs outils (CodeRabbit, Greptile, Cursor BugBot, Copilot agentic, Qodo, Snyk). Principes : reviewer≠auteur, context-first + ticket-aware, signal>recall (what-NOT-to-flag, cap nits, silence), CoT+confiance, vérification « receipts », panel adversarial ciblé, paliers quick/standard/deep, mémoire learnings par repo.
