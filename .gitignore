# Versions archivées des skills — ne jamais publier (peuvent contenir d'anciens exemples)
.archive/
**/.archive/

# Mémoire / réglages PERSO — ne jamais publier (seuls les *.example sont versionnés)
config/memory/MEMORY.md
config/memory/user_profile.md
config/memory/user_env.md
config/settings.local.json

# Résidu d'extraction (le marketplace vit dans plugins/, pas skills/)
/skills/

# Bruit OS / éditeur
.DS_Store
*.swp
.idea/
.vscode/
__pycache__/

# Config Claude vivante : lien depuis ~/.claude/settings.json, contient le contexte
# du poste (orga, domaines, chemins). Le template public est settings.example.json.
config/settings.json
